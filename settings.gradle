pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'CountryRoutingPWC'

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        maven { url = "https://jitpack.io" }
    }
}